import { Styles } from '@/shared/types'

const styles: Styles = {
    settingsColumn: {
        position: { xs: 'sticky', lg: 'relative' },
        top: { xs: 57, sm: 64, md: 71, lg: 0 },
        zIndex: 2,
    },
}

export default styles
