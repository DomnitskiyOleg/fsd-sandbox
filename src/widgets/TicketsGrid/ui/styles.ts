import { Styles } from '@/shared/types'

const styles: Styles = {
    settingsColumn: {
        position: { xs: 'sticky', lg: 'relative' },
        top: { xs: 58, sm: 64, md: 72, lg: 0 },
        zIndex: 2,
    },
}

export default styles
